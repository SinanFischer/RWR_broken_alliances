<?xml version="1.0" encoding="utf-8"?>
<projectile class="grenade" name="stun grenade" slot="0" radius="0.15" key="stun_grenade.projectile">
    <tag name="flashbang" />

    <result class="blast" radius="12.0" damage="0.05" decal="0" character_state="stun" />
    <trigger class="time" time_to_live="3.7" />
    <rotation class="random" />

    <model mesh_filename="stun_grenade.xml" />
    <hud_icon filename="hud_stun_grenade.png" />

    <!-- projectile slot 0 is used for hand grenade items currently -->
    <!-- bound to change -->
    <commonness value="0.008" />
    <capacity value="4" source="rank" source_value="0.0" />
    <capacity value="5" source="rank" source_value="0.15" />
    <capacity value="6" source="rank" source_value="0.35" />
    <inventory encumbrance="0.5" price="2.0" />

    <!-- Wurf: Vanilla + halber Boost (curve war unter Vanilla → leicht zurück Richtung Vanilla) -->
    <throwable curve_height="5.75" near_far_distance="5.0" speed_estimation_near="9.5" speed_estimation_far="17.5" max_speed="15.5" randomness="0.065" />

    <sound class="result" key="other" fileref="stun_grenade1.wav" />

    <sound class="result" key="terrain" copy="other" />
    <sound class="result" key="static_object" copy="other" />
    <sound class="result" key="vehicle" copy="other" />
    <sound class="result" key="character" copy="other" />

    <effect class="result" key="terrain" ref="BigBurst" use_surface_color="1" />
    <effect class="result" key="terrain" ref="FlashShine" lighting="0" />
    <effect class="result" key="terrain" ref="FlashBeam" lighting="0" />

    <effect class="result" key="other" ref="FlashShine" lighting="0" />
    <effect class="result" key="other" ref="FlashBeam" lighting="0" />

    <effect class="result" key="static_object" copy="terrain" />
    <effect class="result" key="vehicle" copy="terrain" />
    <effect class="result" key="character" copy="terrain" />

</projectile>
